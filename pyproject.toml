[project]
name = "canvasmcp"
version = "0.1.0"
description = "Canvas LMS tools for local stdio MCP clients"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.11.1,<3",
    "pydantic>=2.11.7",
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
