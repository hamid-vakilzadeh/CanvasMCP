[project]
name = "canvasmcp"
version = "0.2.0"
description = "Canvas LMS tools for local stdio MCP clients"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp[tasks]==4.0.2",
    "httpx2>=2.5,<3",
    "pydantic>=2.12,<3",
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
